
cmake_minimum_required(VERSION 3.29)

set(math_DIR ${CMAKE_SOURCE_DIR}/lib/math)

find_package(math REQUIRED)
find_package(OpenCV REQUIRED)

project(${ProjectName})

add_executable(${ProjectName} main.cpp)

# 添加eigen头文件路径
target_include_directories(${ProjectName} PRIVATE ${CMAKE_SOURCE_DIR}/lib/eigen-3.4.0 ${CMAKE_SOURCE_DIR}/lib/eigen-3.4.0/Eigen)

# 连接自己的math库
target_link_libraries(${ProjectName} PRIVATE  ${math_libs})

# # ${OpenCV_LIBS}