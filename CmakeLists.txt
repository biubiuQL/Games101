
# find_package(OpenCV REQUIRED)
cmake_minimum_required(VERSION 3.29)

project(${ProjectName})


add_subdirectory(add)

add_executable(${ProjectName} main.cpp)
target_link_libraries(${ProjectName} add )
# ${OpenCV_LIBS}