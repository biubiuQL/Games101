
# find_package(OpenCV REQUIRED)
cmake_minimum_required(VERSION 3.29)

project(${ProjectName})

add_subdirectory(lib/math)

add_executable(${ProjectName} main.cpp)
target_link_libraries(${ProjectName} math)
# ${OpenCV_LIBS}